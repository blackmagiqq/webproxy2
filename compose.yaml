services:
  app:
    build:
      context: .
      dockerfile: ./.docker/golang/Dockerfile
    ports: 
      - "80:3000/tcp"
    volumes:
      - type: bind
        source: .
        target: /var/www/html
